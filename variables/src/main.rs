fn main() {
    let mut x: i32 = 5;
    println!("Number x equals  {}", x);
    x = 6;
    println!("Number x equals {}", x);

    const MAX_POINTS: i32 = 1_000_000;

    println!("MAX POINTS equals {}", MAX_POINTS);
}